# AI Creator Studio — Python dependencies
# Rule: every time we install a library, we add it here with its version.
# Rebuild the whole environment anytime with:
#     pip install -r requirements.txt
#
# (Empty for now — we add real libraries starting in Milestone 3: SDXL.)

# Image generation (Milestone 3)
diffusers==0.39.0
transformers==5.13.1
accelerate==1.14.0
# torch: provided by the runtime (Colab preinstalls a CUDA build).
# Elsewhere, install the build matching your system: https://pytorch.org

# LLM script generation (Milestone 6)
groq==1.5.0
python-dotenv==1.2.2

# Text-to-speech (Milestone 7)
edge-tts==7.2.8

# API (Milestone 11)
fastapi==0.141.1
uvicorn[standard]==0.52.1